//1. High Pass Filter - Given an array and a value cutoff, 
//return a new array containing only the values larger 
//than cutoff.

function highPass(arr, cutoff) {
    var filteredArr = [];
    for(var i = 0; i <= arr.length; i++);{
        if
    }
    return filteredArr;
}
var result = highPass([6, 8, 3, 10, -2, 5, 9], 5);
console.log(result); 





// we expect back [6, 8, 10, 9]

//***************************************************

function createArray(num) {        
    var newArray = [];        
    for (var i = 0; i <= num; i++) {                
        newArray.push(i);        
    }        
    return newArray;        // added the return statement
}
var y = createArray(5); 